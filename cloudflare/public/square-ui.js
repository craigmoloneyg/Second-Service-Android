(()=>{
const money=c=>new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD'}).format((c||0)/100);
async function api(path,options){const r=await fetch(path,{credentials:'same-origin',...options});const j=await r.json();if(!r.ok)throw new Error(j.error||'Square request failed');return j;}
function add(){if(document.getElementById('square-pos'))return;const host=document.querySelector('main')||document.body;const s=document.createElement('section');s.id='square-pos';s.className='card';s.style.marginTop='14px';s.innerHTML=`<div class="card-head"><div><div class="kicker">Live POS integration</div><h2>Square</h2><div class="muted" id="sqSub">Connect Square to bring real sales and menu activity into Price 2 Plate.</div></div><span class="pill blue" id="sqBadge">Checking…</span></div><div id="sqStats" style="display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin:14px 0"></div><div style="display:flex;gap:10px;flex-wrap:wrap"><button class="btn primary" id="sqConnect">Connect Square</button><button class="btn" id="sqSync" hidden>Sync last 30 days</button><button class="btn ghost" id="sqDisconnect" hidden>Disconnect</button></div><div class="muted" id="sqMsg" style="margin-top:12px"></div>`;const anchor=document.getElementById('invoice-processing')||host.firstElementChild;if(anchor&&anchor.parentNode===host)host.insertBefore(s,anchor);else host.appendChild(s);bind();}
function render(x){const connected=!!x.connected,b=document.getElementById('sqBadge'),stats=document.getElementById('sqStats'),snap=x.snapshot;b.textContent=connected?'Connected':'Not connected';b.className='pill '+(connected?'green':'blue');document.getElementById('sqConnect').hidden=connected;document.getElementById('sqSync').hidden=!connected;document.getElementById('sqDisconnect').hidden=!connected;document.getElementById('sqSub').textContent=connected?`Square ${x.environment} connected${x.last_sync_at?' • last sync '+new Date(x.last_sync_at).toLocaleString():''}.`:'Connect Square to bring real sales and menu activity into Price 2 Plate.';stats.innerHTML=snap?`<div><div class="muted">30-day sales</div><strong>${money(snap.gross_sales_30d_cents)}</strong></div><div><div class="muted">Orders</div><strong>${snap.orders_30d||0}</strong></div><div><div class="muted">Square menu items</div><strong>${snap.catalog_items||0}</strong></div><div><div class="muted">Locations</div><strong>${snap.locations?.length||0}</strong></div>`:'';}
async function status(){try{render(await api('/api/square/status'));}catch(e){document.getElementById('sqMsg').textContent=e.message;}}
function bind(){document.getElementById('sqConnect').onclick=async()=>{const m=document.getElementById('sqMsg');try{m.textContent='Opening Square…';const x=await api('/api/square/connect',{method:'POST'});location.href=x.url;}catch(e){m.textContent=e.message;}};document.getElementById('sqSync').onclick=async()=>{const m=document.getElementById('sqMsg');try{m.textContent='Syncing Square sales and menu data…';const x=await api('/api/square/sync',{method:'POST'});m.textContent='Square sync complete.';await status();}catch(e){m.textContent=e.message;}};document.getElementById('sqDisconnect').onclick=async()=>{if(!confirm('Disconnect Square from this workspace?'))return;await api('/api/square/disconnect',{method:'POST'});await status();};status();if(new URLSearchParams(location.search).has('square')){history.replaceState({},'',location.pathname+location.hash);setTimeout(()=>document.getElementById('square-pos')?.scrollIntoView({behavior:'smooth'}),100);}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',add);else add();
})();
